<div id='parent' class='span-24 last'>
  <div id='parent_user' class='span-4'>
    <%= @parent.user.user_name%>
  </div>
  <div id='parent_desc' class='span-20 last'>
    <%= @parent.description%>
  </div>
</div>
<%= form_for(@comment, :url  => create_reply_comment_path(@parent)) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
