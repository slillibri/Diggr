# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
# adapter: sqlite3
# database: db/development.sqlite3  
# pool: 5
# timeout: 5000
development:
  adapter: mysql
  database: diggr_development
  user: digger
  host: localhost
  